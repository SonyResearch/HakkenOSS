# SeGAL model configuration
# Use with: ENV_FILE=.env.segal make serve

MODEL=segal
DEVICE=cuda
CKPT_IS_LIGHTNING=true
RELATIVE_CKPT_PATH=last-v4.ckpt
RELATIVE_PARAMS_PATH=params.json

# Directory mode — uncomment to use (mutually exclusive with MLflow mode)
# RUN_DIR=s3://sai-spaice-ds/models/prod/segal-v1.0.0/

# MLflow mode
MLFLOW_RUN_ID=2d40ff655a9443ba99dbe0975357fa23
ARTIFACT_PATH=checkpoints
TRACKING_URI=http://127.0.0.1:5000
