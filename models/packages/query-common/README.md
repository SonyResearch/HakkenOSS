# Query Common

Common utilities and base classes shared across querying modules.

## Overview

Provides shared abstractions and helpers used by the `simple-query` and `complex-query` packages, avoiding code duplication between querying implementations.

## Installation

```bash
uv sync
```

## Usage

Import shared components:

```python
from query_common import ...
```

## Testing

```bash
uv run pytest
```
