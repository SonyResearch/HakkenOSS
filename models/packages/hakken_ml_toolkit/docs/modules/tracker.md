# Tracker

Tracker is a Python package that provides a flexible and extensible framework for logging and tracking various types of data during experiments or model training. It offers multiple implementations for different tracking backends, allowing users to easily switch between or combine different tracking methods. 